#include <iostream>
using namespace std;
#include <stdio.h>
#include <stdlib.h>
#include <cstdlib>
#include "fonts.h"
#include "log.h"
#include <ctime>
#include <cstring>
#include <cmath>
#include <X11/Xlib.h>
#include <X11/keysym.h>
#include <GL/glx.h>
#include <unistd.h>

void StartMenu()
{
    /*GLuint TitleScreenTexture;

    Image *TitleScreenImage = NULL;

    glBindTexture(GL_TEXTURE_2D, TitleScreenTexture);
    glBegin(GL_QUADS);
        glTexCoord2f(1.0f, 0.0f); glVertex2f( 600.0f, 900.0f);
        glTexCoord2f(1.0f, 1.0f); glVertex2f( 600.0f, 0.0f);
        glTexCoord2f(0.0f, 1.0f); glVertex2f( 0.0f,   0.0f);
        glTexCoord2f(0.0f, 0.0f); glVertex2f( 0.0f,   9000.0f);
    glEnd();
    glBindTexture(GL_TEXTURE_2D, 0);
    glPopMatrix();
    
    glClearColor(0.0, 0.0, 0.0, 1.0);

    Rect s;
    s.bot = 450;
    s.left = 280;
    s.center = 0;
    ggprint16(&s, 16, 0x00ff0000, "FLU FIGHTERS");
    */
}
